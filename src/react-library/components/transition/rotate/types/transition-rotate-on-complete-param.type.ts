import type { IRotate, ScaleDegreesState } from "@react-library/common";

/**
 * Transition rotate component on complete parameters
 */
export type TransitionRotateOnCompleteParam = (
	IRotate<ScaleDegreesState>
);
