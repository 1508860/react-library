import type { TransitionPulseInsetData } from "./transition-pulse-inset-data.type";

/**
 * Transition pulse inset data context result
 */
export type TransitionPulseInsetDataContext = (
	TransitionPulseInsetData |
	undefined
);
