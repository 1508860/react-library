import type { Colour } from "@react-library/common";

/**
 * Describes valid values for the transition border radius component's border colour
 */
export type TransitionBorderRadiusBorderColour = Colour;
