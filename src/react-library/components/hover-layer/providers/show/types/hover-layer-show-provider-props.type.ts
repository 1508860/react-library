import type { PropsWithChildren } from "react";

import type { IShow } from "@react-library/common";

/**
 * Props for hover layer is show provider
 */
export type HoverLayerIsShowProviderProps = (
	PropsWithChildren &
	IShow
);
