import type { PropsWithChildren } from "react";

import type { IIsDisabled } from "@react-library/common";

/**
 * Props for hover layer is disabled provider
 */
export type HoverLayerIsDisabledProviderProps = (
	IIsDisabled &
	PropsWithChildren
);
