import type { ISize } from "@react-library/common";

import type { VirtualScrollItemSize } from "../../../types/virtual-scroll-item-size.type";

/**
 * Props for virtual scroll item register state
 */
export type VirtualScrollItemRegisterState = (
	ISize<VirtualScrollItemSize>
);
