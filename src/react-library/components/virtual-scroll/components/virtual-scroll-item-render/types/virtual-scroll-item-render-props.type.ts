import type { IId } from "@react-library/common";

import type { VirtualScrollItemId } from "../../../types/virtual-scroll-item-id.type";

/**
 * Props for virtual scroll item render component
 */
export type VirtualScrollItemRenderProps = (
	IId<VirtualScrollItemId>
);
