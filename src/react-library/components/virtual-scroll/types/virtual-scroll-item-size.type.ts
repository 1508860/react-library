import type { SizePx } from "@react-library/common";

/**
 * Virtual scroll item size.
 * Unit of measurement for describing the size of item(s) in the virtual scroll
 */
export type VirtualScrollItemSize = SizePx;
