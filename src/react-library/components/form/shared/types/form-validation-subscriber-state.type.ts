import type { SubscriberState } from "@react-library/common";

/**
 * Subscriber state to monitor the state of all form fields and if they are valid or not
 */
export type FormValidationSubscriberState = SubscriberState<boolean>;
