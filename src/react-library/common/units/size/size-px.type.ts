/**
 * Type for a pixel size
 * Note: units are not inculuded as this is the default unit for React and it is expected that this unit is used for element mathmatical operations
 */
export type SizePx = number;

/**
 * Type for a pixel size
 * Note: includes units
 */
export type SizePxUnits = `${number}px`;
