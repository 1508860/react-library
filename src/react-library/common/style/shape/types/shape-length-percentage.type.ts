import type { StyleShapeLengthPercentageSize } from "./shape-length-percentage-size.type";

/**
 * Type to represent the shape units allowed as a pair of coordinates
 */
export type StyleShapeLengthPercentage = `${StyleShapeLengthPercentageSize} ${StyleShapeLengthPercentageSize}`;
