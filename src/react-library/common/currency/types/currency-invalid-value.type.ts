/**
 * Type to define invalid currency value
 * Used for both input and output state
 * Allows consumer to specify if value can be null or undefined
 */
export type CurrencyInvalidValue = (
	null |
	undefined
);
