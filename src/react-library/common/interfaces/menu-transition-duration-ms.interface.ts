/**
 * For a component that can have a menu transition duration in ms
 */
export interface IMenuTransitionDurationMs<TMenuTransitionDurationMs extends number> {
	menuTransitionDurationMs: TMenuTransitionDurationMs;
}
