import type { ILabel } from "@react-library/common";

import type { BadgeLabel } from "./badge-label.type";

/**
 * Props for a badge label
 */
export type BadgePropsLabel = (
	Partial<ILabel<BadgeLabel>>
);
