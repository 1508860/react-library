import type { DateMonthsRange, IDisabledMonths } from "@react-library/common";

/**
 * Props for the date picker disabled months state
 */
export type DatePickerPropsDisabledMonths = (
	Partial<IDisabledMonths<DateMonthsRange>>
);
