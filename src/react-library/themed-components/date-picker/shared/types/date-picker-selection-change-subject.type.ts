import type { Subject } from "@react-library/common";

/**
 * Describes subject for notifying subscribers of a change to the month item index
 */
export type DatePickerSelectionChangeSubject = Subject<number>;
