import type { ISelectedState } from "@react-library/common";

import type { CheckboxSelectedState } from "../enums/checkbox-selected-state.type";

/**
 * Checkbox props relating to the value of a checkbox (checked or unchecked)
 */
export type CheckboxPropsSelectedState = (
	ISelectedState<CheckboxSelectedState>
);
