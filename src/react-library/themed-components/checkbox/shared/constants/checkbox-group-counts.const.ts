import type { CheckboxGroupSelectedCounts } from "../types/checkbox-group-selected-counts.type";

/**
 * Default selected counts for the checkbox group
 */
export const CHECKBOX_GROUP_SELECTED_COUNTS_DEFAULT: CheckboxGroupSelectedCounts = {
	indeterminate: 0,
	selected: 0,
	total: 0
};
