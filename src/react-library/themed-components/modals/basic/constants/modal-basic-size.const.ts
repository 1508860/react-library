import type { SizePx } from "@react-library/common";

/**
 * Defines the amount of padding between items in the header or action components
 */
export const MODAL_BASIC_SIZE_CONTENT_GAP: SizePx = 16;

/**
 * Defines the amount of padding that should surround the modal content
 */
export const MODAL_BASIC_SIZE_CONTENT_PADDING: SizePx = 24;

/**
 * Defines the maximum width of the modal
 */
export const MODAL_BASIC_SIZE_MAX_WIDTH: SizePx = 560;

/**
 * Defines the minimum width of the modal
 */
export const MODAL_BASIC_SIZE_MIN_WIDTH: SizePx = 280;
