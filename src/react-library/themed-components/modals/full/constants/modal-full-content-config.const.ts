import type { ModalContentConfig } from "@react-library/components";

/**
 * Defines the modal content config for the full modal component
 */
export const MODAL_FULL_CONTENT_CONFIG: ModalContentConfig = {
	height: "100%",
	width: "100%"
};
