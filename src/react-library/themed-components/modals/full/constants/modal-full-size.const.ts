import type { SizePx } from "@react-library/common";

/**
 * Defines the amount of padding that should surround the modal content
 */
export const MODAL_FULL_SIZE_CONTENT_PADDING: SizePx = 24;

/**
 * Defines the amount of padding between items in the header component
 */
export const MODAL_FULL_SIZE_HEADER_GAP: SizePx = 16;

/**
 * Defines the height of the header component
 */
export const MODAL_FULL_SIZE_HEADER_PADDING_HEIGHT: SizePx = 64;

/**
 * Defines the amount of horzontal padding for the header component's container
 */
export const MODAL_FULL_SIZE_HEADER_PADDING_HORIZONTAL: SizePx = MODAL_FULL_SIZE_HEADER_GAP;
