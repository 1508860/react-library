import type { PropsWithChildren } from "react";

import type { IShow } from "@react-library/common";

/**
 * Props for modal full container component
 */
export type ModalFullContainerProps = (
	PropsWithChildren &
	IShow
);
