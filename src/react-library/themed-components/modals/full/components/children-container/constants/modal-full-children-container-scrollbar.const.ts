import type { ScrollBasicSizeAll } from "../../../../../shared";

/**
 * Defines the scroll size for the full modal children container
 */
export const MODAL_FULL_CHILDREN_CONTAINER_SCROLLBAR_SIZE: ScrollBasicSizeAll = {
	height: "100%",
	width: "100%"
};
