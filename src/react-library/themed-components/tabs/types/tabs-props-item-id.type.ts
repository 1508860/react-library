import type { IId } from "@react-library/common";

import type { TabsItemId } from "./tabs-item-id.type";

/**
 * Describes the tabs component's props for an item id
 */
export type TabsPropsItemId = (
	IId<TabsItemId>
);
