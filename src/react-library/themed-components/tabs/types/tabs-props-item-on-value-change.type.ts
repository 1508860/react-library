import type { Callback, IOnValueChange } from "@react-library/common";

/**
 * Describes the tabs component's props for an on value change event
 */
export type TabsPropsItemOnValueChange = (
	IOnValueChange<Callback<void>>
);
