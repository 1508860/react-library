import type { IValue } from "@react-library/common";

import type { TabsItemId } from "./tabs-item-id.type";

/**
 * Describes props for the tabs component's value
 */
export type TabsPropsValue = (
	IValue<TabsItemId>
);
