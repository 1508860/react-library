import type { CSSProperties } from "react";

/**
 * Resolve tabs bar content scroll style
 */
export const TABS_BAR_CONTENT_SCROLL_STYLE: CSSProperties = {
	overflowX: "auto",
	scrollbarWidth: "none",
	width: "100%"
};
