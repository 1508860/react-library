import type { IContent } from "@react-library/common";

import type { TooltipRichContent } from "../../../types/tooltip-rich-content.type";

/**
 * Props for tooltip rich content header component
 */
export type TooltipRichContentHeaderProps = (
	IContent<TooltipRichContent>
);
