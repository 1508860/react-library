import type { CSSProperties } from "react";

/**
 * Resolve tooltip rich content buttons style based on parameters
 */
export const TOOLTIP_RICH_CONTENT_BUTTONS_STYLE: CSSProperties = {
	alignItems: "center",
	display: "flex",
	flexDirection: "row",
	justifyContent: "start"
};
