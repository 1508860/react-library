import type { SizePx } from "@react-library/common";

/**
 * Constant to define the rounded border radius for the side sheet panel in px
 */
export const SIDE_SHEET_PANEL_BORDER_RADIUS_ROUND_PX: SizePx = 16;
