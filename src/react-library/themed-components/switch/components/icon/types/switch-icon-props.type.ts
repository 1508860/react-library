import type { SwitchPropsIconName } from "../../../types/switch-props-icon-name.type";
import type { SwitchPropsShowIcon } from "../../../types/switch-props-show-icon.type";

/**
 * Switch icon props
 */
export type SwitchIconProps = (
	SwitchPropsIconName &
	SwitchPropsShowIcon
);
