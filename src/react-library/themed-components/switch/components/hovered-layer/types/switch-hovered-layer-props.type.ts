import type { SwitchPropsIsDisabled } from "../../../types/switch-props-is-disabled.type";

/**
 * Props for switch hovered layer props
 */
export type SwitchHoveredLayerProps = (
	SwitchPropsIsDisabled
);
