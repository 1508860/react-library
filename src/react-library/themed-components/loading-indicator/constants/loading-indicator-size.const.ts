import type { SizePx } from "@react-library/common";

/**
 * Defines the size of the loading indicator background when not collapsed
 */
export const LOADING_INDICATOR_BACKGROUND_SIZE: SizePx = 48;

/**
 * Defines the size of the loading indicator shape when not collapsed
 */
export const LOADING_INDICATOR_SHAPE_SIZE: SizePx = 38;
