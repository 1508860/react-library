import { ScaleDegrees } from "@react-library/common";

/**
 * Defines the amount the loading indicator should rotate for each interval
 */
export const LOADING_INDICATOR_SHAPE_ROTATE: ScaleDegrees = ScaleDegrees[180];
