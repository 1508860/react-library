import type { ReactElement } from "react";

import type { Callback, IMenuElement } from "@react-library/common";

/**
 * Type for props of menu element for the button split component
 */
export type ButtonSplitMenuElement = IMenuElement<Callback<ReactElement>>;
