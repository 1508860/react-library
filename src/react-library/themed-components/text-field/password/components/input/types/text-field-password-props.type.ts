import type { TextFieldPasswordProps } from "../../../types/text-field-password-props.type";

/**
 * Props for text field password component
 */
export type TextFieldPasswordInputProps = (
	TextFieldPasswordProps
);
