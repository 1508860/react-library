import type { ILabel, IName } from "@react-library/common";

/**
 * Text field props relating to input readonly state
 */
export type TextFieldPropsInputReadonly = (
	ILabel<string> &
	IName<string>
);
