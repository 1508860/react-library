import type { SizePx } from "@react-library/common";

/**
 * Text field select menu position margin to determine how far away the menu should be away from the child element
 */
export const TEXT_FIELD_SELECT_MENU_POSITION_MARGIN: SizePx = 8;

/**
 * Text field select menu overlay portal margin to prevent menus from being positioned too closely to the edge of the containing portal
 */
export const TEXT_FIELD_SELECT_MENU_OVERLAY_PORTAL_MARGIN: SizePx = 4;
